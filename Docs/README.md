## Общая информация по компоненте Clip1C

Внешняя компонента 1С для работы с буфером обмена. Содержит один объект
[ClipboardControl](ClipboardControl.md).

Поддерживается как синхронный, так и асинхронный вызов методов компоненты.
Для асинхронного вызова применяются методы:
- НачатьВызов<ИмяМетода>(<ОписаниеОповещения>, <Параметры>)
- НачатьПолучение<ИмяСвойства>(<ОписаниеОповещения>)

Многие свойства возвращают данные в формате JSON. Универсальная функция
разбора:

```bsl
Функция ПрочитатьСтрокуJSON(ТекстJSON)
    Если ПустаяСтрока(ТекстJSON) Тогда
        Возврат Неопределено;
    Иначе
        ЧтениеJSON = Новый ЧтениеJSON();
        ЧтениеJSON.УстановитьСтроку(ТекстJSON);
        Возврат ПрочитатьJSON(ЧтениеJSON);
    КонецЕсли;
КонецФункции
```

### Установка и подключение

Файлы внешней компоненты могут храниться в макете типа «двоичные данные».
Стандартный шаблон подключения:

```bsl
&НаКлиенте
Перем ИдентификаторКомпоненты, БуферОбмена;

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    МакетКомпоненты = РеквизитФормыВЗначение("Объект").ПолучитьМакет("Clip1C");
    МестоположениеКомпоненты = ПоместитьВоВременноеХранилище(МакетКомпоненты, УникальныйИдентификатор);
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
    ИдентификаторКомпоненты = "_" + СтрЗаменить(Новый УникальныйИдентификатор, "-", "");
    НачатьПодключениеВнешнейКомпоненты(
        Новый ОписаниеОповещения("ПодключениеВнешнейКомпонентыЗавершение", ЭтаФорма),
        МестоположениеКомпоненты, ИдентификаторКомпоненты, ТипВнешнейКомпоненты.Native);
КонецПроцедуры

&НаКлиенте
Процедура ПодключениеВнешнейКомпонентыЗавершение(Подключение, ДополнительныеПараметры) Экспорт
    Если Подключение Тогда
        БуферОбмена = Новый("AddIn." + ИдентификаторКомпоненты + ".ClipboardControl");
    КонецЕсли;
КонецПроцедуры
```

### Подключение на сервере

```bsl
&НаСервереБезКонтекста
Процедура ПодключитьНаСервере()
    ПодключитьВнешнююКомпоненту(
        "ОбщийМакет.Clip1C",
        "Clip1C",
        ТипВнешнейКомпоненты.Native,
        ТипПодключенияВнешнейКомпоненты.НеИзолированно);
    БуферОбмена = Новый("AddIn.Clip1C.ClipboardControl");
КонецПроцедуры
```

***

[Инструкция по самостоятельной сборке библиотеки](BuildLibrary.md)

При разработке использовались сторонние библиотеки:
- [Clip Library by David Capello](https://github.com/dacap/clip) — буфер обмена под Linux
- [JSON for Modern C++ by Niels Lohmann](https://github.com/nlohmann/json) — сериализация JSON
